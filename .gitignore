# Python 虚拟环境
.venv/
__pycache__/

# 数据文件 (绝对不能传)
*.csv
*.npy
*.pkl
data/

# The daily forecast store IS committed — it is the site's data and must persist across the
# ephemeral CI runners. Re-allow it past the blanket *.csv / data/ ignores above.
!/web/
!/web/*.csv

# 模型权重和结果
/models/
/results/

# 日志文件
logs/
*.out
*.err